def find_probability(N, P):
dp=[0.0]*(N+1)
    dp[0]=1.0
    for i in range(2,N+1):
        p2=dp[i-2]*P
        p3=dp[i-3]*(1-P) if i>= 3 else 0
        dp[i]=p2+p3
    return dp
N, P = input().split()
N = int(N)
P = float(P)
print(f"{find_probability(N, P):.2f}",end="")
